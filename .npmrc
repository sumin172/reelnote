# Hoist @types packages and common type dependencies to make them accessible to TypeScript
# This is essential for TypeScript to resolve types in pnpm's strict mode
public-hoist-pattern[]=@types/*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=chalk

# Hoist workspace dependencies to root for better type resolution
shamefully-hoist=false
