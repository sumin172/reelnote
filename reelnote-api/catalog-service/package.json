{
  "name": "catalog-service",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nx serve catalog-service",
    "start": "nx serve catalog-service",
    "test": "nx test catalog-service",
    "generate:openapi": "tsx --tsconfig tsconfig.openapi.json src/scripts/generate-openapi.ts",
    "format:check": "prettier --check . --no-color",
    "format:write": "prettier --write .",
    "eslint": "eslint . --cache --no-color",
    "lint": "pnpm run format:check && pnpm eslint"
  },
  "dependencies": {
    "prom-client": "^15.1.3"
  },
  "prisma": {
    "seed": "ts-node --transpile-only prisma/seed.ts"
  },
  "devDependencies": {
    "@types/pg": "^8.15.6",
    "pg": "^8.16.3",
    "tsx": "^4.20.6"
  }
}
